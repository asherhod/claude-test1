# Mangalist — Decision Log

## 2026-07-07 — Local cutover to "Fire & Heat" (`web/`) complete, QA-verified, no known bugs
- Completes the cutover approved earlier the same day (see entry directly below). CIO → Dev → QA pipeline finished: `web/dist` (production build, not the Vite dev server) is now served at http://localhost:5500 via `npx serve -s web/dist -l 5500`, with SPA-fallback so all 6 client-side routes work on direct load/refresh. Old shell (`index.html`, `catalog.html`, `product.html`, `styles.css`, `script.js`, `catalog.js`, `product.js`, `catalog-shared.js`, `supabase-config.js`, `serve.json`) retired to `legacy/` (git history preserved via `git mv` for the 3 previously-tracked files; nothing committed yet). `tools/prerender-products.js` updated to the new `/product/:id` URL shape and to read config from `web/.env.local` instead of the retired `supabase-config.js`. `CLAUDE.md`'s "Running locally"/"Structure"/Notes sections updated to match.
- QA independently re-verified against the actual production build being served (not `npm run dev`, per the redesign plan's Migration Risk #1) — full RLS/publish-gating matrix, all 6 routes incl. direct-load/refresh on the new SPA-fallback config, Shop filters, cart flow incl. the variant-pricing fix, checkout-stub zero-network-requests check, contact form, and the updated pre-render script's output — all passed. First-ever mobile/responsive + basic accessibility smoke pass (open gap flagged 2026-07-06 twice) found 2 real bugs: no mobile nav menu below 720px (regression vs. the old legacy site), and an RTL/bidi rendering defect reversing the visual order of the product-dimensions text. Both routed back to Dev, fixed (mobile hamburger-toggle nav; `dir="ltr"` on the dimensions span), rebuilt, and independently re-verified by QA — clean pass, no known bugs, logged in `qa.md`.
- Demo data (3 products + Char-Broil brand + gas-grills category, published temporarily for the parent's preview) reverted to the canonical baseline (`published=false`, `badge`/`is_featured` cleared) as part of QA's final pass, confirmed via both `psql` and the production build's rendered empty state. CIO independently spot-checked the final DB state and the running server before reporting this complete — 0 published rows anywhere across all 5 catalog tables, 0 leads, homepage/shop both serving 200 with the new app.
- **Still local-only.** Nothing was deployed to `mangalist.co.il` or AWS — that remains a separate, unapproved future decision. No new cost introduced by this pass.

## 2026-07-07 — Parent approves cutover: "Fire & Heat" redesign (`web/`) to replace the old static shell
- After viewing both sites side by side locally with demo data published (see below), parent approved cutover per the redesign plan's §8 gate ("the new design looks awesome, lets do it and replace the old").
- **Scope: local cutover only.** This makes the `web/` React build the site served locally in place of the old `index.html`/`catalog.html`/`product.html` static shell. It is **not** a deploy to the real `mangalist.co.il` domain or AWS — that remains a separate, still-unapproved future decision.
- Routed to CIO to execute per plan §8 step 4: production build (`web/dist/`) becomes what's served locally; old root files retired to a `legacy/` folder (not deleted) as a one-release rollback safety net; `CLAUDE.md`'s "Running locally" section updated to match.
- Known open gap (mobile/responsive + accessibility, flagged 2026-07-06, still unaddressed) folded into this pass — QA to do at least a basic smoke check before sign-off, since this build is about to become the actual site rather than a side-by-side preview.
- Demo data published temporarily for the parent's live preview before this approval (3 of 7 seeded Char-Broil products, real-looking prices/stock/badges) — must be reverted back to the canonical all-`published=false` baseline once the cutover build is confirmed working, per the same rule QA has enforced all along (no real pricing/supplier deal exists yet).
- Awaiting CIO/Dev/QA completion report before this is presented as done.

## 2026-07-07 — Cart variant-pricing bug (found 2026-07-06) fixed and QA-verified
- Closes out the one open item from the 2026-07-06 "Fire & Heat" redesign full-app QA pass: cart lines were showing a product's cheapest variant price instead of the specific variant the customer selected on Product Detail.
- CIO routed the fix to Dev: cart is now keyed by `productId::variantId` (was bare `productId`), so a product can hold distinct cart lines per variant, each priced against its own real RLS-filtered price. Storage key bumped `mangalist_cart_v1` → `mangalist_cart_v2` (no migration needed, local-only app). Changes confined to `web/` (`CartContext.jsx`, `catalogShared.js`, `ProductCard.jsx`, `ProductDetail.jsx`, `Cart.jsx`).
- QA independently re-verified (real Playwright/Chromium, real Supabase test data, reviewable SQL setup/teardown): original repro fixed, plus new two-lines-per-product and variant-level stray-cart-id cases, plus a full re-run of the other Cart scenarios (empty state, free-shipping threshold, whole-product stray id, checkout-stub zero-network-requests, header badge, Product Detail add-to-cart). All 10 scenario groups passed, no bugs found, all test data reverted to canonical baseline. See `qa.md`'s 2026-07-07 entry.
- Still 100% local/unpublished — no cost, no new decision, no parent approval needed (bug fix on already-in-progress work, not a new commitment or a cutover step).
- **Still open, not part of this fix:** mobile/responsive layout and accessibility have not been tested on the `web/` redesign — flagged again from the 2026-07-06 pass, needs a CEO/parent call on whether to cover before any go-live/cutover discussion.

## 2026-07-06 — Local Supabase stack re-verified after Docker Desktop restart; DB publish-state fixed
- Docker Desktop had been stopped since the 2026-07-04/07-05 build sessions; parent restarted it and `npx supabase start` brought the same persisted stack back up (no data loss — same Docker volumes reused).
- CIO re-confirmed `supabase-config.js` still matches the live stack's real API URL/anon key — no change needed.
- CIO found the local DB in an unexpected state: all 7 seeded products/variants, the 1 brand, and the 1 category were `published = true`, almost certainly from the parent toggling checkboxes while exploring Supabase Studio's table editor (2026-07-05). This didn't match any approved state — no supplier deal is closed and no real pricing exists yet, so nothing was authorized to be publicly visible. Restored the canonical `published = false` baseline via `npx supabase db reset` (re-applies migrations + seed.sql).
- CIO then re-verified end-to-end: contact form → `leads` insert works and lands correctly; RLS still enforces insert-only on `leads` and publish-gated read on the 5 catalog tables; catalog/product pages correctly show empty/not-found states with nothing published.
- QA independently re-verified from scratch (real Playwright browser session, not a re-check of CIO's own findings): contact form real-browser submit, full RLS matrix, `catalog.html`/`product.html` empty/not-found states, and a full publish→verify→unpublish→verify round-trip proving the gating genuinely works post-restart. **No bugs found** — see `qa.md`.
- Still 100% local/unpublished — no live-traffic exposure at any point during this check. No new cost, no deploy step, no parent approval needed for this verification work itself.

## 2026-07-04 — Business defined, virtual CEO structure established
- Business: Mangalist, gas grills & BBQ accessories, online-only, Israel. Domain mangalist.co.il already owned.
- Team: twins (21, post-army) run day-to-day ops (orders, delivery, supplier deals). Parent funds and holds final financial sign-off. Claude Code acts as virtual CEO (ops/marketing/tech/finance oversight, advisory only on money).
- Budget: 30,000–50,000 NIS available now.
- Decided to defer building skills/subagents until real operational load exists (see `strategy.md`), rather than pre-building an ops/marketing/tech/finance agent org with no orders or suppliers yet.
- Decided to rebuild the site's existing structure in Hebrew/RTL as a shell first, deferring full product catalog + checkout until suppliers and a payment provider are chosen.

## 2026-07-04 — Brand decision locked: Char-Broil + Napoleon
- Parent approved launching with two brands: Char-Broil (entry/mid tier, TRU-Infrared) and Napoleon (premium tier), spanning two price points without overextending the 30-50k NIS budget across four brands.
- Weber (oversaturated Israeli market) and Broil King (overlaps Napoleon's tier) deprioritized for launch — revisit once Char-Broil/Napoleon are established.
- Next step: twins contact the importer leads in `suppliers.md` (Explore for Char-Broil; Grill Town/Carmel Direct/Grill Master for Napoleon) to negotiate reseller/wholesale terms. No terms confirmed yet — this decision locks the *brands*, not any actual supplier agreement or spend.

## 2026-07-04 — Payment/shipping/fulfillment recommendations prepared (awaiting parent review)
- While parent was away, prepared (not decided) recommendations for the three remaining open items in `strategy.md`: payment gateway (leaning Tranzila), shipping (get freight-specialist quotes for bulky grills, e.g. HFD/OneStop, rather than committing to one courier), and fulfillment model (order-on-demand rather than holding inventory at launch, given budget and no sales data yet).
- Nothing signed or committed — all three are flagged for parent review/approval before any real commitment.

## 2026-07-04 — CTO replaced with CIO (broader technology scope)
- Parent asked to remove the CTO subagent and replace it with a CIO / VP Technologies (סמנכ"ל טכנולוגיות), responsible for all technology matters explicitly including information security, code quality, and the architecture of the technical solution — broader than the original CTO's website/tech-roadmap framing.
- Removed `.claude/agents/cto.md`; added `.claude/agents/cio.md`. Updated all cross-references in `CLAUDE.md`, `ceo/strategy.md`, `.claude/agents/marketing.md`, `.claude/agents/coo.md`.

## 2026-07-04 — Full C-suite stood up (CFO, COO, CTO, Marketing & Sales)
- Parent clarified the intended structure further: full staff of four subagents, each owning one domain — CFO (finance/reporting), COO (operations: purchases, delivery, customer satisfaction, plus grill product/technical evaluation for supplier support), CTO (all technology), Marketing & Sales (brand, copy, channels, sales targets).
- Staff can and should consult each other directly on ongoing/business matters; the CEO provides direction and only brings a plan to the parent once satisfied with it; the parent is the sole decision-maker for execution on everything, not just financial matters.
- Added `.claude/agents/cfo.md`, `.claude/agents/coo.md`, expanded `.claude/agents/marketing.md` to cover sales, updated `.claude/agents/cto.md` cross-references. New `ceo/operations.md` operating doc for the COO.
- Also fixed an RTL rendering bug in the live-preview artifact (missing `dir="rtl"` wrapper on the preview copy) — the actual repo `index.html` was already correct (`dir="rtl"` on the `<html>` tag).

## 2026-07-04 — Operating process formalized; Marketing subagent added
- Parent directed that the CEO must actually operate as CEO: direct the CTO/Marketing subagents to execute work rather than doing it all directly, let staff consult each other on cross-domain questions (e.g. Marketing → CTO on technical feasibility), then review whether execution succeeded before bringing the resulting decision to the parent.
- Nothing is final/live/implemented until the parent explicitly approves — generalizes the existing financial-approval rule to all final decisions, not just money. Documented in `../CLAUDE.md` under a new "Operating process" section.
- Added a Marketing subagent (`.claude/agents/marketing.md`), ahead of the original "defer until real recurring work" plan — parent explicitly wants marketing topics routed through this role now.

## 2026-07-04 — Local Supabase build complete, QA-verified, parent approved
- Completes the "Phase 1 site architecture" build approved earlier the same day, previously blocked on Docker Desktop/WSL2 not being installed on the parent's machine. Parent installed Docker Desktop + WSL2 (with a restart in between); local Supabase stack (Postgres + PostgREST + Studio + Auth) started successfully via `npx supabase start`, and `supabase-config.js` was filled in with the real local API URL + anon key.
- Contact form (`index.html`/`script.js`) now does a real insert into `public.leads` instead of the old client-side-only stub.
- QA independently verified (see `qa.md`): real insert path works (tested via the actual `supabase-js` call the site makes), RLS correctly enforced (anon can insert leads but not read them back; products only readable where `published = true`, currently none), site serves the correct homepage, catalog section still shows the static "coming soon" note. No bugs found; one non-bug footgun flagged for Dev (don't chain `.select()` after `.insert()` on `leads` — anon has no SELECT grant, so it would error).
- Parent independently confirmed by submitting the form themselves in the browser before the CEO's own sanity check — both landed in the `leads` table as expected.
- **Nothing is live or deployed** — this remains 100% local. Deploying the same docker-compose to AWS (est. 60–150 NIS/month, within the already-approved 150–450 NIS/month band) is a separate future decision requiring its own explicit parent sign-off.

## 2026-07-05 — Catalogue management site: plan ready for review (not yet approved)
- Parent tried Supabase Studio's raw table editor for adding products and found it not user-friendly; asked for a proper catalogue management site for the twins, plan first, no build yet.
- CIO directed Solution Architect + DBA to produce a plan at `ceo/design/catalogue-management-design.md`. Scope: guided admin pages (login, brands, categories, products, a product-edit wizard) on the existing plain HTML/CSS/JS + self-hosted Supabase pattern — orders/checkout untouched.
- **Access-control approach (the real design question, CIO personally reviewed rather than rubber-stamping)**: Supabase Auth (email/password) for two twin accounts, gated by an explicit `staff_users` allow-list + a `security definer` `is_staff()` helper (deliberately not a bare "any authenticated user" check, to avoid silently granting future customer accounts catalog-write access). New staff-only write RLS policies added per table; existing anon/published-only read policies untouched. `service_role` key confirmed never present in client-side code. CIO caught and fixed one flawed self-referencing RLS policy in the DBA's draft, and flagged a real gap: uploaded product images would live in a Docker volume not covered by the existing Postgres backup script — needs closing before real photos go live.
- **No new cost** — Auth/Storage/Postgres are all already running in the previously-approved local stack; only future cost flagged is a real SMTP provider if/when this deploys (staff password-reset emails), not needed now.
- Proposed phased build order: (1) auth + brand/category CRUD, (2) product+variant guided form, (3) image upload (closing the backup gap first), (4) ongoing list/edit views — each phase through DBA → Dev → QA as usual.
- **Awaiting parent's review of the plan (access-control approach + phase order) before any implementation starts** — nothing built yet.

## 2026-07-05 — Product catalogue built and QA-verified (still local, still unpublished)
- Completes the implementation approved earlier the same day. DBA → Dev → QA pipeline finished: new `brands`/`categories`/`products`/`product_variants`/`product_images` schema + RLS (migration `20260705090000_create_catalog_schema.sql`), the 7 seeded Char-Broil rows migrated into the new shape, `catalog.html`/`product.html` built (browse/filter/search/variant picker/stock badges), and the Option B pre-render script (`tools/prerender-products.js`) producing static per-product HTML with real title/meta/OG tags.
- QA signed off with **no known bugs** (full detail in `qa.md`) — RLS cascade, schema/seed integrity, filter/search/facet/URL behavior, product page states, and pre-render output all independently verified via a real headless-browser session. Two real bugs Dev caught and fixed itself during the build (nested-anchor breaking product cards; a dead-looking price-on-request link) were re-confirmed fixed by QA.
- **Nothing is live or deployed** — same as the local Supabase build before it, all catalog rows still `published = false`, no live-traffic risk.
- **Three items explicitly left open, not decided by CIO/Dev/DBA:** (1) exact Hebrew stock-badge wording and the product/variant slug convention — Marketing's call; (2) `mangalist.co.il` used as a placeholder canonical domain in pre-render OG tags, pending a real hosting decision; (3) `catalog-prerendered/` committed as a normal tracked directory (regenerate-and-recommit) rather than gitignored — flagged in case a different deploy story is preferred once real hosting is decided.

## 2026-07-05 — Product catalogue design approved for implementation
- Parent reviewed `ceo/design/product-catalogue-design.pdf` (Solution Architect + DBA joint design, display/browsing catalogue only) and approved moving to implementation. No new paid service/cost is introduced anywhere in the design.
- The one open decision the design left to the parent: whether to build the SEO/link-preview static pre-rendering step (Option B in the design) now, or defer it. **Parent chose to build it now**, not defer — product pages will get real `<title>`/meta/Open Graph tags via a small build-time Node.js generator script from day one, rather than waiting for an active traffic/sharing plan.
- All other recommendations in the design's §6 sign-off table stand as written (Postgres full-text search, no dedicated search service; Supabase Storage for images, no CDN; real slugs for brands/categories now, id/sku routing for products until Marketing settles a Hebrew slug convention).
- CIO directed to kick off implementation: DBA writes the schema migration (brands/categories/products/product_variants/product_images + RLS + indexes, migrating the 7 existing seeded Char-Broil rows), Dev builds `catalog.html`/`product.html` plus the pre-render script, QA verifies before anything is reported done. Stays local/unpublished — no live/deploy step happens as part of this.

## 2026-07-05 — Nadav joins as a manager member
- Parent brought Nadav (one of the twins) in to learn how to work with Galisto directly, and directed that Nadav join as a manager member of the company alongside the parent and the virtual CEO.
- Clarified scope: no new approval authority — the parent remains the sole sign-off on any financial/execution decision. Nadav's change is purely about being able to directly direct Galisto and the subagents for day-to-day work, the same way the parent or Galisto itself does.
- Built an onboarding "course" (interactive walkthrough) for Nadav covering the org structure, how to phrase requests to Galisto, and the approval boundary. Updated `CLAUDE.md` and `ceo/strategy.md` team sections accordingly.

## 2026-07-05 — Solution Architect and DBA added under CIO
- Parent (Galisto's principal) directed that the CIO's team gets two further direct reports: **Solution Architect** (technical design/architecture for significant new builds, evaluates tradeoffs, produces a design spec before Dev implements) and **DBA** (owns the Postgres/Supabase data layer — schema/migrations, indexing, RLS correctness, backup/restore).
- Added `.claude/agents/solution-architect.md` and `.claude/agents/dba.md`. CIO now manages four direct reports (Solution Architect, DBA, Dev, QA) instead of two. Updated `.claude/agents/cio.md`, `CLAUDE.md`, and `ceo/strategy.md` accordingly.
- Rationale: the local Supabase build so far had Dev writing schema/RLS directly with no dedicated data-layer owner, and no separate design step before implementation — reasonable at trivial scale but a gap as the AWS deploy and future checkout/order data model approach.

## 2026-07-04 — CTO subagent added
- Added a CTO role (`.claude/agents/cto.md`) under the virtual CEO, scoped specifically to website & tech roadmap (stack decisions, catalog/checkout path, payment/shipping integration, hosting, security) — chosen over product/technical grill evaluation or house-brand R&D since the website is the one workstream with real ongoing work today.
- Product/technical evaluation of grill models (to support supplier negotiations) and future product-line R&D remain unassigned for now; revisit if they become recurring needs.
